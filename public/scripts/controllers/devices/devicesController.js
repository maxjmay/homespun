/*global Homespun, angular, $ */

Homespun.controller('DevicesController', ['$scope', '$rootScope', '$http', '$location',
	function devicesController($scope, $rootScope, $http, $location) {
		'use strict';

		if ($location.path() === '/devices') {

		} else if ($location.path() === '/device') {

		}

}]);
